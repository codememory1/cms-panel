## Установка проекта

#### Сборка контейнеров
```sh
> docker-compose build
```

#### Поднятие контейнеров
```sh
> docker-compose up -d
```

#### Установка зависимостей
```sh
> bin/composer install
```